# References

Run this from the skill root to generate the local reference files:

```bash
node scripts/build_catalog_reference.js
```

Generated catalog files are intentionally ignored by git because they are derived from third-party Dragon Herbs catalog content. The public repository includes the matching logic and guidance, while each user builds their own local searchable reference.
